# Flask environment
FLASK_ENV=production

# Database (SQLite used locally, can be updated for production DB)
DATABASE_URL=sqlite:///detection_database.db

# Optional: API settings
API_PORT=5000

# Optional: For frontend deployed separately
REACT_APP_API_URL=http://localhost:5000/api
